{% extends 'base.html.twig' %}

{% block title %}{% endblock %}

{% block body %}
    <div>
        <div>
            <h1>bienvenus!</h1>
            <p>Plongez dans un monde de sensations glacées et de défis vertigineux avec notre site dédié aux snowtricks : où la passion pour la neige rencontre l'adrénaline de la performance !</p>
        </div>
    </div>

    <div class="row">
        {% for figure in figures %}
            <div class="col-2 ">
                <div class="card">
                    <img src="..." class="card-img-top" alt="...">

                    <div class="card-body text-end">
                        <a href="{{ path('figure_details', {'id': figure.id}) }}" class="me-3">{{ figure.name }}</a>
                        <a href="#" class="me-3"><i class="fa-solid fa-pencil text-dark"></i></a>
                        <a href="#" class=""><i class="fa-solid fa-trash-can text-dark"></i></a>
                    </div>
                </div>
            </div>
        {% endfor %}
    </div>

{% endblock %}
